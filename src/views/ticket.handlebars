<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" id="home">Home</a>
        </li>
      </ul>
      <div><button id="logout">Log out</button></div>
    </div>
  </div>
</nav>


<div id="app">
    <h1 id="productId">Ticket with the ID:  {{ticket.code}}  </h1>
  

    <!--div id="productDetail" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3">
        <div id="product-{{ticket._id}}" class="max-w-sm border rounded-lg shadow bg-gray-800 border-gray-700 m-1">
            <div>
                {{#each ticket.arrayproductscomprados}}
                <img src="{{this}}" alt="product image" />
                {{/each}}
            </div>
            <div>
                <h5 id="code"> code: {{ticket.code}}</h5>
               
                <p>Amount $:
                    {{ticket.amount}}</p>
                <p>Purcharser:
                    {{ticket.purcharser}}</p>
                <p>Purchase_datetime:
                    {{ticket.purchase_datetime}}</p>
               

                    
                   

                    <div>
                         <label for="cid">Cart ID</label>
                        <input placeholder=" " type="text" name="cid" id="cid" value={{cId}}  disabled/>
                         <button id="botonForm">Add to Cart </button>
                    </div>
           
            </div>
        </div>
    </div-->
</div>

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script src="/js/ticket.js"></script>